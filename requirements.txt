# FSAR Dependencies

# Memory
chromadb>=0.4

# MCP support
mcp>=1.0,<2

# Communication
websockets>=12.0
fastapi>=0.110
uvicorn[standard]>=0.27
python-telegram-bot>=21.0
lark-oapi>=1.7.1

# Utilities
pyyaml>=6.0
python-dotenv>=1.0
loguru>=0.7
rich>=13.0       # CLI Markdown rendering (Phase 2.5)

# Image handling — avatar upload in src/memory/cards.py and src/server/ws_server.py
Pillow>=10.0

# PDF processing
PyPDF2>=3.0

# Web requests
httpx>=0.24
edge-tts>=6.1
faster-whisper>=1.0

# Provider SDKs — used by src/utils/llm_factory
anthropic>=0.39
google-genai>=0.3,<2
openai>=1.109.1

cryptography>=42.0

# Computer Use (cua PyPI library — pulls cua-auto for direct host control)
cua>=0.1.6

# Scheduler (Phase 1 — APScheduler-based background scheduling)
APScheduler>=3.10
croniter>=2.0

# Tests — async scheduler tests (Task 6)
pytest-asyncio>=0.23
